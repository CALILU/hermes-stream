{
  "name": "hermes-stream",
  "version": "1.0.0",
  "description": "Streaming de video desde servidor FTP con transcodificación en tiempo real",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd my-ui && npm install && npm run build",
    "postbuild": "echo 'Frontend compilado en my-ui/build'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "nixpacks": {
    "pkgs": [
      "ffmpeg"
    ]
  },
  "dependencies": {
    "axios": "^1.13.2",
    "basic-ftp": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "fluent-ffmpeg": "^2.1.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
